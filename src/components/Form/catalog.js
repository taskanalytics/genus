import {
  Input,
} from '.'

import { Flex, Box } from '../Grid'

export default {
  title: 'Form',
  path: '/components/form',
  imports: { Input, Flex, Box },
  component: require('./README.md'),
}
