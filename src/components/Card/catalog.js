import Card from '.'
import Heading from '../Heading'
import Text from '../Text'
import Button from '../Button'
import { Box, Flex } from '../Grid'

export default {
  title: 'Card',
  path: '/components/card',
  imports: { Card, Heading, Text, Box, Button, Flex },
  component: require('./README.md'),
}
