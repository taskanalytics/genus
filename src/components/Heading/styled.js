import styled from "@emotion/styled";
import {
  width,
  space,
  color,
  textAlign,
  fontSize,
  fontWeight,
  lineHeight,
} from 'styled-system'

export const StyledHeading = styled('h1')({
  margin: 0,
},
width,
space,
color,
textAlign,
fontWeight,
fontSize,
lineHeight
)
